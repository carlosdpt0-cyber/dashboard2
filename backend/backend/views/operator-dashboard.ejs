<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard Operador - Casino B7uno' %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #1a237e;
            --secondary-color: #283593;
            --accent-color: #3949ab;
            --success-color: #4caf50;
            --warning-color: #ff9800;
            --danger-color: #f44336;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --border-radius: 10px;
            --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: var(--dark-color);
        }
        
        .dashboard-container {
            padding: 20px;
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .dashboard-header {
            background: white;
            border-radius: var(--border-radius);
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: var(--box-shadow);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .header-left h1 {
            color: var(--primary-color);
            margin: 0;
            font-size: 1.8rem;
            font-weight: 700;
        }
        
        .header-left .breadcrumb {
            margin: 5px 0 0;
            color: #666;
            font-size: 0.9rem;
        }
        
        .header-right {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 15px;
            background: var(--light-color);
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .user-info:hover {
            background: #e9ecef;
        }
        
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary-color);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
        }
        
        .user-details h6 {
            margin: 0;
            font-weight: 600;
        }
        
        .user-details small {
            color: #666;
            font-size: 0.8rem;
        }
        
        .logout-btn {
            background: var(--danger-color);
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .logout-btn:hover {
            background: #d32f2f;
            transform: translateY(-2px);
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: white;
            border-radius: var(--border-radius);
            padding: 20px;
            box-shadow: var(--box-shadow);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
        }
        
        .stat-card.tickets::before {
            background: var(--warning-color);
        }
        
        .stat-card.emails::before {
            background: var(--success-color);
        }
        
        .stat-card.assigned::before {
            background: var(--accent-color);
        }
        
        .stat-card.total::before {
            background: var(--primary-color);
        }
        
        .stat-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 15px;
            font-size: 1.5rem;
            color: white;
        }
        
        .stat-card.tickets .stat-icon {
            background: var(--warning-color);
        }
        
        .stat-card.emails .stat-icon {
            background: var(--success-color);
        }
        
        .stat-card.assigned .stat-icon {
            background: var(--accent-color);
        }
        
        .stat-card.total .stat-icon {
            background: var(--primary-color);
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            margin: 5px 0;
            color: var(--dark-color);
        }
        
        .stat-label {
            color: #666;
            font-size: 0.9rem;
            margin: 0;
        }
        
        .stat-change {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 0.8rem;
            margin-top: 10px;
        }
        
        .stat-change.positive {
            color: var(--success-color);
        }
        
        .stat-change.negative {
            color: var(--danger-color);
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .content-card {
            background: white;
            border-radius: var(--border-radius);
            padding: 25px;
            box-shadow: var(--box-shadow);
        }
        
        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--light-color);
        }
        
        .card-header h3 {
            margin: 0;
            color: var(--primary-color);
            font-size: 1.3rem;
            font-weight: 600;
        }
        
        .card-header .badge {
            background: var(--primary-color);
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
        }
        
        .table-responsive {
            overflow-x: auto;
        }
        
        .tickets-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .tickets-table th {
            text-align: left;
            padding: 12px 15px;
            background: var(--light-color);
            color: var(--dark-color);
            font-weight: 600;
            font-size: 0.9rem;
            border-bottom: 2px solid #dee2e6;
        }
        
        .tickets-table td {
            padding: 15px;
            border-bottom: 1px solid #dee2e6;
            vertical-align: middle;
        }
        
        .tickets-table tr:hover {
            background: #f8f9fa;
        }
        
        .priority-badge {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
        }
        
        .priority-low {
            background: #d4edda;
            color: #155724;
        }
        
        .priority-medium {
            background: #fff3cd;
            color: #856404;
        }
        
        .priority-high {
            background: #f8d7da;
            color: #721c24;
        }
        
        .priority-urgent {
            background: #dc3545;
            color: white;
        }
        
        .status-badge {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        
        .status-open {
            background: #fff3cd;
            color: #856404;
        }
        
        .status-in_progress {
            background: #cce5ff;
            color: #004085;
        }
        
        .status-resolved {
            background: #d4edda;
            color: #155724;
        }
        
        .status-closed {
            background: #f8f9fa;
            color: #6c757d;
        }
        
        .action-btn {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 6px 12px;
            border-radius: 5px;
            cursor: pointer;
            transition: var(--transition);
            font-size: 0.85rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .action-btn:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
        }
        
        .email-list {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .email-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px;
            border-radius: 8px;
            background: var(--light-color);
            transition: var(--transition);
            cursor: pointer;
        }
        
        .email-item:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }
        
        .email-icon {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: var(--accent-color);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }
        
        .email-content {
            flex: 1;
        }
        
        .email-subject {
            font-weight: 600;
            margin-bottom: 5px;
            color: var(--dark-color);
        }
        
        .email-meta {
            display: flex;
            justify-content: space-between;
            font-size: 0.85rem;
            color: #666;
        }
        
        .email-status {
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        
        .status-sent {
            background: #d4edda;
            color: #155724;
        }
        
        .status-pending {
            background: #fff3cd;
            color: #856404;
        }
        
        .status-failed {
            background: #f8d7da;
            color: #721c24;
        }
        
        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 30px;
        }
        
        .quick-action-btn {
            background: white;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            padding: 15px;
            border-radius: var(--border-radius);
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            text-align: center;
        }
        
        .quick-action-btn:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            text-decoration: none;
        }
        
        .quick-action-btn i {
            font-size: 1.5rem;
        }
        
        .quick-action-btn span {
            font-weight: 600;
            font-size: 0.9rem;
        }
        
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }
        
        .modal-content {
            background: white;
            border-radius: var(--border-radius);
            padding: 30px;
            max-width: 500px;
            width: 90%;
            max-height: 80vh;
            overflow-y: auto;
        }
        
        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--light-color);
        }
        
        .modal-header h3 {
            margin: 0;
            color: var(--primary-color);
        }
        
        .close-modal {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: #666;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--dark-color);
        }
        
        .form-control {
            width: 100%;
            padding: 10px 15px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1rem;
            transition: var(--transition);
        }
        
        .form-control:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(26, 35, 126, 0.1);
        }
        
        .btn-primary {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: 600;
            width: 100%;
        }
        
        .btn-primary:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
        }
        
        .notifications-badge {
            position: relative;
            cursor: pointer;
        }
        
        .notifications-badge .badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--danger-color);
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
        }
        
        .notification-dropdown {
            position: absolute;
            top: 60px;
            right: 20px;
            background: white;
            border-radius: var(--border-radius);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            width: 300px;
            max-height: 400px;
            overflow-y: auto;
            display: none;
            z-index: 1000;
        }
        
        .notification-header {
            padding: 15px;
            border-bottom: 2px solid var(--light-color);
            font-weight: 600;
            color: var(--primary-color);
        }
        
        .notification-item {
            padding: 15px;
            border-bottom: 1px solid #eee;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .notification-item:hover {
            background: #f8f9fa;
        }
        
        .notification-item.unread {
            background: #e3f2fd;
        }
        
        .notification-title {
            font-weight: 600;
            margin-bottom: 5px;
            color: var(--dark-color);
        }
        
        .notification-message {
            color: #666;
            font-size: 0.9rem;
            margin-bottom: 5px;
        }
        
        .notification-time {
            font-size: 0.8rem;
            color: #999;
        }
        
        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #666;
        }
        
        .empty-state i {
            font-size: 3rem;
            color: #ccc;
            margin-bottom: 15px;
        }
        
        .empty-state p {
            margin: 0;
        }
        
        .loading-spinner {
            display: none;
            justify-content: center;
            padding: 20px;
        }
        
        .loading-spinner i {
            font-size: 2rem;
            color: var(--primary-color);
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            border-left: 5px solid;
        }
        
        .alert-success {
            background: #d4edda;
            border-color: #155724;
            color: #155724;
        }
        
        .alert-error {
            background: #f8d7da;
            border-color: #721c24;
            color: #721c24;
        }
        
        .alert-warning {
            background: #fff3cd;
            border-color: #856404;
            color: #856404;
        }
        
        @media (max-width: 768px) {
            .dashboard-container {
                padding: 10px;
            }
            
            .dashboard-header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            
            .header-right {
                width: 100%;
                justify-content: center;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .content-grid {
                grid-template-columns: 1fr;
            }
            
            .content-card {
                padding: 15px;
            }
            
            .quick-actions {
                grid-template-columns: 1fr;
            }
            
            .notification-dropdown {
                width: 90%;
                right: 5%;
            }
        }
    </style>
</head>
<body>
    <!-- Modal de Confidencialidade -->
    <div id="confidentialityModal" class="modal-overlay" style="display: <%= acceptedConfidentiality ? 'none' : 'flex' %>;">
        <div class="modal-content">
            <div class="modal-header">
                <h3><i class="fas fa-shield-alt"></i> Termos de Confidencialidade</h3>
            </div>
            <div class="modal-body">
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                    <strong>Atenção:</strong> Para aceder ao sistema, deve aceitar os termos de confidencialidade.
                </div>
                
                <div style="max-height: 300px; overflow-y: auto; padding: 15px; background: #f8f9fa; border-radius: 8px; margin-bottom: 20px;">
                    <h4>ACORDO DE CONFIDENCIALIDADE DO CASINO B7UNO</h4>
                    <p><strong>Data de Efeito:</strong> <%= new Date().toLocaleDateString('pt-PT') %></p>
                    
                    <h5>1. OBJETIVO</h5>
                    <p>Este Acordo de Confidencialidade ("Acordo") tem como objetivo proteger informações confidenciais do Casino B7UNO ("Empresa") a que terá acesso durante a sua atividade laboral.</p>
                    
                    <h5>2. INFORMAÇÕES CONFIDENCIAIS</h5>
                    <p>Consideram-se Informações Confidenciais todos os dados, informações e materiais, incluindo mas não limitados a:</p>
                    <ul>
                        <li>Dados de jogadores e clientes</li>
                        <li>Informações financeiras e transacionais</li>
                        <li>Sistemas e processos internos</li>
                        <li>Estratégias comerciais e planos de negócio</li>
                        <li>Qualquer informação marcada como "Confidencial" ou equivalente</li>
                    </ul>
                    
                    <h5>3. OBRIGAÇÕES DO FUNCIONÁRIO</h5>
                    <p>O Funcionário compromete-se a:</p>
                    <ul>
                        <li>Manter a confidencialidade de todas as Informações Confidenciais</li>
                        <li>Utilizar as Informações Confidenciais apenas para fins autorizados</li>
                        <li>Não divulgar, copiar ou distribuir Informações Confidenciais</li>
                        <li>Proteger o acesso às Informações Confidenciais com senhas seguras</li>
                        <li>Reportar imediatamente qualquer violação de segurança</li>
                    </ul>
                    
                    <h5>4. DURAÇÃO</h5>
                    <p>Este Acordo permanece em vigor durante o período de emprego e por 5 anos após a sua cessação.</p>
                    
                    <h5>5. CONSEQUÊNCIAS DE VIOLAÇÃO</h5>
                    <p>A violação deste Acordo pode resultar em ação disciplinar, incluindo rescisão do contrato e ações legais.</p>
                    
                    <div style="text-align: center; margin-top: 20px; padding: 15px; background: #e3f2fd; border-radius: 8px; border-left: 5px solid #2196f3;">
                        <i class="fas fa-lock" style="font-size: 2rem; color: #2196f3; margin-bottom: 10px;"></i>
                        <p style="margin: 0;"><strong>Confidencialidade é a nossa prioridade máxima.</strong><br>
                        Ao aceitar, compromete-se a proteger a informação do Casino B7UNO.</p>
                    </div>
                </div>
                
                <div class="form-group">
                    <label style="display: flex; align-items: center; gap: 10px; cursor: pointer;">
                        <input type="checkbox" id="agreeCheckbox">
                        <span>Li e concordo com os Termos de Confidencialidade do Casino B7UNO</span>
                    </label>
                </div>
                
                <button id="acceptConfidentialityBtn" class="btn-primary" disabled>
                    <i class="fas fa-check-circle"></i> Aceitar e Continuar
                </button>
            </div>
        </div>
    </div>

    <!-- Modal de Notificações -->
    <div id="notificationsModal" class="modal-overlay">
        <div class="modal-content">
            <div class="modal-header">
                <h3><i class="fas fa-bell"></i> Notificações</h3>
                <button class="close-modal" onclick="closeNotificationsModal()">&times;</button>
            </div>
            <div class="modal-body">
                <div id="notificationsList">
                    <div class="loading-spinner">
                        <i class="fas fa-spinner fa-spin"></i>
                    </div>
                </div>
            </div>
            <div class="modal-footer" style="text-align: center; padding-top: 15px; border-top: 2px solid var(--light-color);">
                <button id="markAllReadBtn" class="action-btn" style="margin: 0 auto;">
                    <i class="fas fa-check-double"></i> Marcar Todas como Lidas
                </button>
            </div>
        </div>
    </div>

    <!-- Container Principal -->
    <div class="dashboard-container">
        <!-- Header -->
        <div class="dashboard-header">
            <div class="header-left">
                <h1><i class="fas fa-tachometer-alt"></i> Dashboard Operador</h1>
                <div class="breadcrumb">
                    <i class="fas fa-home"></i> Home > Dashboard Operador
                </div>
            </div>
            
            <div class="header-right">
                <div class="notifications-badge" onclick="openNotificationsModal()">
                    <i class="fas fa-bell" style="font-size: 1.5rem; color: var(--primary-color);"></i>
                    <span class="badge" id="notificationCount">0</span>
                </div>
                
                <div class="user-info" onclick="window.location.href='/profile'">
                    <div class="user-avatar">
                        <% if (user.photo) { %>
                            <img src="/uploads/<%= user.photo %>" alt="Foto" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
                        <% } else { %>
                            <%= user.name.charAt(0).toUpperCase() %>
                        <% } %>
                    </div>
                    <div class="user-details">
                        <h6><%= user.name %></h6>
                        <small>Operador</small>
                    </div>
                </div>
                
                <button class="logout-btn" onclick="logout()">
                    <i class="fas fa-sign-out-alt"></i> Sair
                </button>
            </div>
        </div>

        <!-- Alertas -->
        <% if (success && success.length > 0) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i> <%= success %>
            </div>
        <% } %>
        
        <% if (error && error.length > 0) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-circle"></i> <%= error %>
            </div>
        <% } %>

        <!-- Estatísticas -->
        <div class="stats-grid">
            <div class="stat-card tickets">
                <div class="stat-icon">
                    <i class="fas fa-ticket-alt"></i>
                </div>
                <h3>Tickets Abertos</h3>
                <div class="stat-value"><%= stats.openTickets %></div>
                <p class="stat-label">Tickets a aguardar resposta</p>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i> 12% esta semana
                </div>
            </div>
            
            <div class="stat-card assigned">
                <div class="stat-icon">
                    <i class="fas fa-user-check"></i>
                </div>
                <h3>Atribuídos a Mim</h3>
                <div class="stat-value"><%= stats.assignedTickets %></div>
                <p class="stat-label">Tickets atribuídos a si</p>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i> 3 novos hoje
                </div>
            </div>
            
            <div class="stat-card emails">
                <div class="stat-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <h3>Emails Enviados</h3>
                <div class="stat-value"><%= stats.recentEmails %></div>
                <p class="stat-label">Emails enviados por si</p>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i> 8% este mês
                </div>
            </div>
            
            <div class="stat-card total">
                <div class="stat-icon">
                    <i class="fas fa-clipboard-list"></i>
                </div>
                <h3>Total de Tickets</h3>
                <div class="stat-value"><%= stats.totalTickets %></div>
                <p class="stat-label">Todos os tickets do sistema</p>
                <div class="stat-change negative">
                    <i class="fas fa-arrow-down"></i> 2% desde ontem
                </div>
            </div>
        </div>

        <!-- Conteúdo Principal -->
        <div class="content-grid">
            <!-- Tickets Recentes -->
            <div class="content-card">
                <div class="card-header">
                    <h3><i class="fas fa-ticket-alt"></i> Tickets Recentes</h3>
                    <span class="badge"><%= recentTickets.length %> tickets</span>
                </div>
                
                <div class="table-responsive">
                    <% if (recentTickets && recentTickets.length > 0) { %>
                        <table class="tickets-table">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Jogador</th>
                                    <th>Assunto</th>
                                    <th>Prioridade</th>
                                    <th>Status</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% recentTickets.forEach(ticket => { %>
                                    <tr>
                                        <td><strong><%= ticket.ticketId %></strong></td>
                                        <td><%= ticket.playerName %></td>
                                        <td><%= ticket.subject %></td>
                                        <td>
                                            <% 
                                                let priorityClass = '';
                                                let priorityText = '';
                                                switch(ticket.priority) {
                                                    case 'low':
                                                        priorityClass = 'priority-low';
                                                        priorityText = 'Baixa';
                                                        break;
                                                    case 'medium':
                                                        priorityClass = 'priority-medium';
                                                        priorityText = 'Média';
                                                        break;
                                                    case 'high':
                                                        priorityClass = 'priority-high';
                                                        priorityText = 'Alta';
                                                        break;
                                                    case 'urgent':
                                                        priorityClass = 'priority-urgent';
                                                        priorityText = 'Urgente';
                                                        break;
                                                    default:
                                                        priorityClass = 'priority-medium';
                                                        priorityText = 'Média';
                                                }
                                            %>
                                            <span class="priority-badge <%= priorityClass %>">
                                                <%= priorityText %>
                                            </span>
                                        </td>
                                        <td>
                                            <% 
                                                let statusClass = '';
                                                let statusText = '';
                                                switch(ticket.status) {
                                                    case 'open':
                                                        statusClass = 'status-open';
                                                        statusText = 'Aberto';
                                                        break;
                                                    case 'in_progress':
                                                        statusClass = 'status-in_progress';
                                                        statusText = 'Em Progresso';
                                                        break;
                                                    case 'resolved':
                                                        statusClass = 'status-resolved';
                                                        statusText = 'Resolvido';
                                                        break;
                                                    case 'closed':
                                                        statusClass = 'status-closed';
                                                        statusText = 'Fechado';
                                                        break;
                                                    default:
                                                        statusClass = 'status-open';
                                                        statusText = 'Aberto';
                                                }
                                            %>
                                            <span class="status-badge <%= statusClass %>">
                                                <%= statusText %>
                                            </span>
                                        </td>
                                        <td>
                                            <button class="action-btn" onclick="viewTicket('<%= ticket._id %>')">
                                                <i class="fas fa-eye"></i> Ver
                                            </button>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <i class="fas fa-inbox"></i>
                            <p>Nenhum ticket encontrado</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Emails Recentes -->
            <div class="content-card">
                <div class="card-header">
                    <h3><i class="fas fa-envelope"></i> Emails Recentes</h3>
                    <span class="badge"><%= recentEmails.length %> emails</span>
                </div>
                
                <div class="email-list">
                    <% if (recentEmails && recentEmails.length > 0) { %>
                        <% recentEmails.forEach(email => { %>
                            <div class="email-item">
                                <div class="email-icon">
                                    <i class="fas fa-paper-plane"></i>
                                </div>
                                <div class="email-content">
                                    <div class="email-subject"><%= email.subject %></div>
                                    <div class="email-meta">
                                        <span><%= email.playersCount %> destinatários</span>
                                        <span><%= new Date(email.sentAt).toLocaleDateString('pt-PT') %></span>
                                    </div>
                                </div>
                                <div class="email-status status-sent">
                                    <%= email.status === 'sent' ? 'Enviado' : email.status %>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="empty-state">
                            <i class="fas fa-envelope-open"></i>
                            <p>Nenhum email enviado recentemente</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Ações Rápidas -->
        <div class="quick-actions">
            <a href="/support" class="quick-action-btn">
                <i class="fas fa-ticket-alt"></i>
                <span>Ver Todos os Tickets</span>
            </a>
            
            <a href="/email" class="quick-action-btn">
                <i class="fas fa-envelope"></i>
                <span>Enviar Email</span>
            </a>
            
            <a href="/support?status=open" class="quick-action-btn">
                <i class="fas fa-exclamation-circle"></i>
                <span>Tickets Abertos</span>
            </a>
            
            <a href="/profile" class="quick-action-btn">
                <i class="fas fa-user-cog"></i>
                <span>Meu Perfil</span>
            </a>
        </div>
    </div>

    <script>
        // ============================================
        // TERMOS DE CONFIDENCIALIDADE
        // ============================================
        
        // Verificar estado dos termos
        document.addEventListener('DOMContentLoaded', function() {
            const agreeCheckbox = document.getElementById('agreeCheckbox');
            const acceptBtn = document.getElementById('acceptConfidentialityBtn');
            const confidentialityModal = document.getElementById('confidentialityModal');
            
            // Ativar/desativar botão baseado no checkbox
            if (agreeCheckbox && acceptBtn) {
                agreeCheckbox.addEventListener('change', function() {
                    acceptBtn.disabled = !this.checked;
                });
                
                // Lidar com aceitação dos termos
                acceptBtn.addEventListener('click', async function() {
                    if (!agreeCheckbox.checked) return;
                    
                    acceptBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> A processar...';
                    acceptBtn.disabled = true;
                    
                    try {
                        const response = await fetch('/api/confidentiality/accept', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            }
                        });
                        
                        const data = await response.json();
                        
                        if (data.success) {
                            // Fechar modal
                            confidentialityModal.style.display = 'none';
                            
                            // Mostrar mensagem de sucesso
                            showAlert('Termos aceitos com sucesso! Pode agora aceder a todas as funcionalidades.', 'success');
                            
                            // Atualizar sessão
                            if (typeof updateSessionConfidentiality === 'function') {
                                updateSessionConfidentiality(true);
                            }
                        } else {
                            throw new Error(data.error || 'Erro ao aceitar termos');
                        }
                    } catch (error) {
                        console.error('Erro:', error);
                        showAlert('Erro ao aceitar termos: ' + error.message, 'error');
                        acceptBtn.innerHTML = '<i class="fas fa-check-circle"></i> Aceitar e Continuar';
                        acceptBtn.disabled = false;
                    }
                });
            }
            
            // Carregar notificações iniciais
            loadNotifications();
        });
        
        // ============================================
        // NOTIFICAÇÕES
        // ============================================
        
        let notifications = [];
        
        async function loadNotifications() {
            try {
                const response = await fetch('/api/notifications');
                const data = await response.json();
                
                if (data.success) {
                    notifications = data.notifications || [];
                    updateNotificationCount();
                    renderNotifications();
                }
            } catch (error) {
                console.error('Erro ao carregar notificações:', error);
            }
        }
        
        function updateNotificationCount() {
            const unreadCount = notifications.filter(n => !n.read).length;
            const badge = document.getElementById('notificationCount');
            if (badge) {
                badge.textContent = unreadCount;
                badge.style.display = unreadCount > 0 ? 'flex' : 'none';
            }
        }
        
        function renderNotifications() {
            const container = document.getElementById('notificationsList');
            if (!container) return;
            
            if (notifications.length === 0) {
                container.innerHTML = `
                    <div class="empty-state">
                        <i class="fas fa-bell-slash"></i>
                        <p>Nenhuma notificação</p>
                    </div>
                `;
                return;
            }
            
            let html = '';
            notifications.forEach(notification => {
                const timeAgo = getTimeAgo(notification.createdAt);
                const icon = getNotificationIcon(notification.type);
                
                html += `
                    <div class="notification-item ${notification.read ? '' : 'unread'}" onclick="markNotificationAsRead('${notification.id}')">
                        <div class="notification-title">
                            <i class="fas fa-${icon}"></i> ${notification.title}
                        </div>
                        <div class="notification-message">
                            ${notification.message}
                        </div>
                        <div class="notification-time">
                            ${timeAgo}
                        </div>
                    </div>
                `;
            });
            
            container.innerHTML = html;
        }
        
        function getTimeAgo(dateString) {
            const date = new Date(dateString);
            const now = new Date();
            const diffMs = now - date;
            const diffMins = Math.floor(diffMs / 60000);
            const diffHours = Math.floor(diffMins / 60);
            const diffDays = Math.floor(diffHours / 24);
            
            if (diffMins < 1) return 'Agora mesmo';
            if (diffMins < 60) return `Há ${diffMins} minuto${diffMins > 1 ? 's' : ''}`;
            if (diffHours < 24) return `Há ${diffHours} hora${diffHours > 1 ? 's' : ''}`;
            if (diffDays < 7) return `Há ${diffDays} dia${diffDays > 1 ? 's' : ''}`;
            return date.toLocaleDateString('pt-PT');
        }
        
        function getNotificationIcon(type) {
            switch(type) {
                case 'info': return 'info-circle';
                case 'warning': return 'exclamation-triangle';
                case 'danger': return 'exclamation-circle';
                case 'success': return 'check-circle';
                default: return 'bell';
            }
        }
        
        function openNotificationsModal() {
            const modal = document.getElementById('notificationsModal');
            modal.style.display = 'flex';
            renderNotifications();
        }
        
        function closeNotificationsModal() {
            const modal = document.getElementById('notificationsModal');
            modal.style.display = 'none';
        }
        
        async function markNotificationAsRead(notificationId) {
            try {
                const response = await fetch(`/api/notifications/${notificationId}/read`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                const data = await response.json();
                if (data.success) {
                    // Atualizar notificação localmente
                    const notification = notifications.find(n => n.id === notificationId);
                    if (notification) {
                        notification.read = true;
                    }
                    updateNotificationCount();
                    renderNotifications();
                }
            } catch (error) {
                console.error('Erro ao marcar notificação como lida:', error);
            }
        }
        
        document.getElementById('markAllReadBtn')?.addEventListener('click', async function() {
            try {
                const response = await fetch('/api/notifications/mark-all-read', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                const data = await response.json();
                if (data.success) {
                    // Marcar todas como lidas localmente
                    notifications.forEach(n => n.read = true);
                    updateNotificationCount();
                    renderNotifications();
                    showAlert('Todas as notificações foram marcadas como lidas', 'success');
                }
            } catch (error) {
                console.error('Erro ao marcar todas como lidas:', error);
                showAlert('Erro ao marcar notificações como lidas', 'error');
            }
        });
        
        // ============================================
        // FUNÇÕES UTILITÁRIAS
        // ============================================
        
        function viewTicket(ticketId) {
            window.location.href = `/support/ticket/${ticketId}`;
        }
        
        function logout() {
            if (confirm('Tem a certeza que deseja terminar sessão?')) {
                window.location.href = '/logout';
            }
        }
        
        function showAlert(message, type = 'success') {
            // Remover alertas existentes
            const existingAlerts = document.querySelectorAll('.alert');
            existingAlerts.forEach(alert => alert.remove());
            
            // Criar novo alerta
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type}`;
            alertDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-circle'}"></i>
                ${message}
            `;
            
            // Inserir após o header
            const header = document.querySelector('.dashboard-header');
            if (header) {
                header.parentNode.insertBefore(alertDiv, header.nextSibling);
            }
            
            // Remover após 5 segundos
            setTimeout(() => {
                if (alertDiv.parentNode) {
                    alertDiv.remove();
                }
            }, 5000);
        }
        
        // Fechar modal ao clicar fora
        window.addEventListener('click', function(event) {
            const confidentialityModal = document.getElementById('confidentialityModal');
            const notificationsModal = document.getElementById('notificationsModal');
            
            if (confidentialityModal && event.target === confidentialityModal) {
                // Não permitir fechar o modal de confidencialidade clicando fora
                // O utilizador DEVE aceitar os termos
            }
            
            if (notificationsModal && event.target === notificationsModal) {
                notificationsModal.style.display = 'none';
            }
        });
        
        // Atualizar dados periodicamente (a cada 30 segundos)
        setInterval(() => {
            loadNotifications();
        }, 30000);
    </script>
</body>
</html>