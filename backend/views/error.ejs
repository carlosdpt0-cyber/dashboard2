<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Erro | B7Uno Casino Admin' %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg-primary: #0a0a0a;
            --bg-secondary: #111111;
            --bg-tertiary: #1a1a1a;
            --bg-surface: rgba(255, 215, 0, 0.03);
            --text-primary: #ffffff;
            --text-secondary: #cccccc;
            --text-tertiary: #888888;
            --gold-primary: #D4AF37;
            --gold-secondary: #FFD700;
            --gold-light: #F5D042;
            --gold-dark: #B8860B;
            --gold-accent: #CFB53B;
            --border-color: rgba(212, 175, 55, 0.15);
            --border-light: rgba(212, 175, 55, 0.08);
            --shadow-lg: 0 20px 60px rgba(0, 0, 0, 0.7);
            --shadow-gold: 0 0 30px rgba(212, 175, 55, 0.2);
            --radius-lg: 16px;
            --radius-md: 12px;
            --radius-sm: 8px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #000000 0%, #0a0a0a 100%);
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
            line-height: 1.6;
            font-weight: 400;
        }

        .error-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 24px;
            position: relative;
            background: 
                radial-gradient(circle at 20% 50%, rgba(212, 175, 55, 0.05) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 215, 0, 0.03) 0%, transparent 50%);
        }

        .error-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                repeating-linear-gradient(
                    90deg,
                    transparent,
                    transparent 2px,
                    rgba(212, 175, 55, 0.02) 2px,
                    rgba(212, 175, 55, 0.02) 4px
                );
            pointer-events: none;
        }

        .error-content {
            background: var(--bg-secondary);
            border-radius: var(--radius-lg);
            padding: 48px;
            max-width: 920px;
            width: 100%;
            box-shadow: var(--shadow-lg);
            border: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .error-content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--gold-primary), var(--gold-secondary), var(--gold-light));
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.5);
        }

        .error-content::after {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.1) 0%, transparent 70%);
            border-radius: 50%;
            z-index: -1;
        }

        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 40px;
            padding-bottom: 24px;
            border-bottom: 1px solid var(--border-light);
        }

        .brand-info {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .brand-logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 24px;
            font-weight: 700;
            color: var(--gold-primary);
            text-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
        }

        .brand-logo i {
            color: var(--gold-secondary);
            font-size: 28px;
        }

        .system-tag {
            background: rgba(212, 175, 55, 0.15);
            color: var(--gold-light);
            padding: 6px 12px;
            border-radius: var(--radius-sm);
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 0.5px;
            border: 1px solid rgba(212, 175, 55, 0.3);
        }

        .error-id {
            color: var(--gold-accent);
            font-size: 14px;
            font-family: 'Monaco', 'Menlo', monospace;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .error-id i {
            color: var(--gold-secondary);
        }

        .error-main {
            text-align: center;
            margin-bottom: 48px;
            position: relative;
        }

        .error-icon-wrapper {
            position: relative;
            width: 100px;
            height: 100px;
            margin: 0 auto 32px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .error-icon-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            background: var(--gold-primary);
            border-radius: 50%;
            opacity: 0.1;
            filter: blur(15px);
            animation: pulse 2s infinite ease-in-out;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.1; transform: scale(1); }
            50% { opacity: 0.2; transform: scale(1.1); }
        }

        .error-icon {
            font-size: 48px;
            color: var(--gold-primary);
            background: var(--bg-tertiary);
            width: 80px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            border: 2px solid rgba(212, 175, 55, 0.4);
            box-shadow: var(--shadow-gold);
            z-index: 2;
        }

        .error-code {
            font-size: 96px;
            font-weight: 900;
            line-height: 1;
            margin-bottom: 16px;
            color: rgba(212, 175, 55, 0.05);
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 0;
            user-select: none;
            text-shadow: 0 0 20px rgba(212, 175, 55, 0.1);
        }

        <% if (error && error.status) { %>
            .error-code::before {
                content: '<%= error.status %>';
            }
        <% } else { %>
            .error-code::before {
                content: '500';
            }
        <% } %>

        .error-title {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 16px;
            color: var(--gold-primary);
            position: relative;
            z-index: 1;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
        }

        .error-message {
            font-size: 18px;
            color: var(--text-secondary);
            max-width: 640px;
            margin: 0 auto 40px;
            line-height: 1.7;
            padding: 0 20px;
        }

        .error-details-section {
            background: var(--bg-surface);
            border-radius: var(--radius-md);
            padding: 32px;
            margin-bottom: 40px;
            border: 1px solid var(--border-light);
            position: relative;
        }

        .error-details-section h3 {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 24px;
            color: var(--gold-light);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .error-details-section h3 i {
            color: var(--gold-primary);
        }

        .error-meta-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 32px;
        }

        .meta-card {
            background: var(--bg-tertiary);
            padding: 20px;
            border-radius: var(--radius-md);
            border: 1px solid var(--border-light);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .meta-card:hover {
            border-color: var(--gold-primary);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
        }

        .meta-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: linear-gradient(to bottom, var(--gold-primary), var(--gold-secondary));
        }

        .meta-label {
            font-size: 13px;
            color: var(--gold-accent);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .meta-value {
            font-size: 16px;
            color: var(--text-primary);
            font-weight: 500;
            word-break: break-word;
        }

        .status-code {
            color: var(--gold-secondary) !important;
            font-weight: 700;
        }

        .stack-trace {
            background: var(--bg-tertiary);
            padding: 24px;
            border-radius: var(--radius-md);
            margin-top: 24px;
            border: 1px solid var(--border-light);
        }

        .stack-trace summary {
            cursor: pointer;
            font-weight: 600;
            color: var(--gold-light);
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 15px;
            transition: color 0.3s;
        }

        .stack-trace summary:hover {
            color: var(--gold-secondary);
        }

        .stack-trace pre {
            background: var(--bg-secondary);
            padding: 20px;
            border-radius: var(--radius-sm);
            overflow-x: auto;
            font-size: 13px;
            color: var(--gold-accent);
            border: 1px solid var(--border-light);
            margin-top: 16px;
            font-family: 'Monaco', 'Menlo', 'Consolas', monospace;
            line-height: 1.5;
            tab-size: 2;
        }

        .action-buttons {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            margin-bottom: 40px;
        }

        .btn {
            padding: 16px 24px;
            border-radius: var(--radius-md);
            text-decoration: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            transition: all 0.3s ease;
            border: 1px solid transparent;
            font-size: 15px;
            cursor: pointer;
            text-align: center;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, var(--gold-primary), var(--gold-secondary));
            opacity: 0;
            transition: opacity 0.3s;
            z-index: -1;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--gold-primary), var(--gold-secondary));
            color: #000000;
            font-weight: 700;
            border: none;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(212, 175, 55, 0.4);
        }

        .btn-secondary {
            background: var(--bg-tertiary);
            color: var(--gold-light);
            border-color: var(--gold-primary);
        }

        .btn-secondary:hover {
            background: linear-gradient(135deg, var(--gold-primary), var(--gold-secondary));
            color: #000000;
            border-color: transparent;
            transform: translateY(-3px);
        }

        .btn-outline {
            background: transparent;
            color: var(--gold-light);
            border-color: var(--border-color);
        }

        .btn-outline:hover {
            background: rgba(212, 175, 55, 0.1);
            border-color: var(--gold-primary);
            color: var(--gold-secondary);
            transform: translateY(-3px);
        }

        .footer-section {
            padding-top: 32px;
            border-top: 1px solid var(--border-light);
            color: var(--text-tertiary);
            font-size: 14px;
        }

        .footer-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 24px;
            margin-bottom: 24px;
        }

        .footer-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 8px 0;
        }

        .footer-item i {
            color: var(--gold-secondary);
            width: 16px;
        }

        .footer-item strong {
            color: var(--gold-light);
            font-weight: 600;
        }

        .security-notice {
            text-align: center;
            font-size: 13px;
            color: var(--gold-accent);
            padding: 16px;
            background: rgba(212, 175, 55, 0.05);
            border-radius: var(--radius-md);
            border: 1px solid rgba(212, 175, 55, 0.1);
            margin-top: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
        }

        .gold-divider {
            height: 1px;
            background: linear-gradient(90deg, 
                transparent, 
                var(--gold-primary), 
                var(--gold-secondary), 
                var(--gold-primary), 
                transparent);
            margin: 32px 0;
            opacity: 0.5;
        }

        @media (max-width: 768px) {
            .error-content {
                padding: 32px 24px;
            }

            .error-title {
                font-size: 28px;
            }

            .error-code {
                font-size: 72px;
            }

            .error-message {
                font-size: 16px;
            }

            .error-meta-grid {
                grid-template-columns: 1fr;
            }

            .action-buttons {
                grid-template-columns: 1fr;
            }

            .footer-info {
                grid-template-columns: 1fr;
            }

            .header-section {
                flex-direction: column;
                gap: 16px;
            }
        }

        /* Efeitos de brilho dourado */
        .gold-glow {
            animation: goldGlow 3s infinite alternate;
        }

        @keyframes goldGlow {
            from { box-shadow: 0 0 20px rgba(212, 175, 55, 0.3); }
            to { box-shadow: 0 0 30px rgba(212, 175, 55, 0.5), 0 0 40px rgba(212, 175, 55, 0.2); }
        }
    </style>
</head>
<body class="error-page">
    <div class="error-container">
        <div class="error-content gold-glow">
            <div class="header-section">
                <div class="brand-info">
                    <div class="brand-logo">
                        <i class="fas fa-crown"></i>
                        B7Uno Casino
                    </div>
                    <div class="system-tag">
                        <i class="fas fa-shield-alt"></i> ADMIN PREMIUM
                    </div>
                </div>
                <div class="error-id">
                    <i class="fas fa-gem"></i> 
                    ID: <%= Math.random().toString(36).substring(2, 10).toUpperCase() %>
                </div>
            </div>

            <div class="error-main">
                <div class="error-code"></div>
                
                <div class="error-icon-wrapper">
                    <div class="error-icon-bg"></div>
                    <div class="error-icon">
                        <% if (error && error.status === 404) { %>
                            <i class="fas fa-search"></i>
                        <% } else if (error && error.status === 403) { %>
                            <i class="fas fa-ban"></i>
                        <% } else if (error && error.status === 401) { %>
                            <i class="fas fa-lock"></i>
                        <% } else { %>
                            <i class="fas fa-exclamation-triangle"></i>
                        <% } %>
                    </div>
                </div>
                
                <h1 class="error-title">
                    <% if (error && error.status === 404) { %>
                        Página Não Encontrada
                    <% } else if (error && error.status === 403) { %>
                        Acesso Restrito
                    <% } else if (error && error.status === 401) { %>
                        Não Autorizado
                    <% } else { %>
                        Erro do Sistema
                    <% } %>
                </h1>
                
                <p class="error-message">
                    <% if (error && error.status === 404) { %>
                        O recurso solicitado não foi encontrado no sistema B7Uno. 
                        Verifique o URL e tente novamente.
                    <% } else if (error && error.status === 403) { %>
                        Acesso negado. Você não possui permissões para acessar esta área do sistema.
                    <% } else if (error && error.status === 401) { %>
                        Sessão expirada. Por favor, autentique-se novamente para continuar.
                    <% } else { %>
                        <%= message || 'Ocorreu um erro inesperado no sistema B7Uno. A equipe técnica foi notificada.' %>
                    <% } %>
                </p>
            </div>

            <div class="gold-divider"></div>

            <div class="error-details-section">
                <h3>
                    <i class="fas fa-info-circle"></i> 
                    Detalhes Técnicos
                </h3>
                
                <div class="error-meta-grid">
                    <% if (error && error.status) { %>
                        <div class="meta-card">
                            <div class="meta-label">
                                <i class="fas fa-barcode"></i> Código
                            </div>
                            <div class="meta-value">
                                <span class="status-code"><%= error.status %></span>
                                <span style="color: var(--gold-accent);">
                                    <% if (error.status === 404) { %>• Não Encontrado<% } %>
                                    <% if (error.status === 403) { %>• Acesso Negado<% } %>
                                    <% if (error.status === 401) { %>• Não Autorizado<% } %>
                                    <% if (error.status === 500) { %>• Erro Interno<% } %>
                                </span>
                            </div>
                        </div>
                    <% } %>
                    
                    <% if (error && error.timestamp) { %>
                        <div class="meta-card">
                            <div class="meta-label">
                                <i class="fas fa-clock"></i> Horário
                            </div>
                            <div class="meta-value">
                                <%= new Date(error.timestamp).toLocaleString('pt-PT', { 
                                    year: 'numeric', 
                                    month: '2-digit', 
                                    day: '2-digit',
                                    hour: '2-digit',
                                    minute: '2-digit',
                                    second: '2-digit'
                                }) %>
                            </div>
                        </div>
                    <% } %>
                    
                    <div class="meta-card">
                        <div class="meta-label">
                            <i class="fas fa-bolt"></i> Status
                        </div>
                        <div class="meta-value">
                            <span style="color: var(--gold-secondary);">
                                <%= process.env.NODE_ENV === 'production' ? '🟢 PRODUÇÃO' : '🟡 DESENVOLVIMENTO' %>
                            </span>
                        </div>
                    </div>
                    
                    <div class="meta-card">
                        <div class="meta-label">
                            <i class="fas fa-crown"></i> Sistema
                        </div>
                        <div class="meta-value">
                            B7Uno Admin 
                        </div>
                    </div>
                </div>

                <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
                    <div class="stack-trace">
                        <details>
                            <summary>
                                <i class="fas fa-code"></i> Stack Trace (Modo Desenvolvimento)
                            </summary>
                            <pre><%= error.stack %></pre>
                        </details>
                    </div>
                <% } %>
            </div>

            <div class="action-buttons">
                <a href="/dashboard" class="btn btn-primary">
                    <i class="fas fa-tachometer-alt"></i> Dashboard Principal
                </a>
                
                <% if (error && (error.status === 401 || error.status === 403)) { %>
                    <a href="/auth/login" class="btn btn-secondary">
                        <i class="fas fa-key"></i> Autenticar
                    </a>
                <% } %>
                
                <a href="javascript:history.back()" class="btn btn-outline">
                    <i class="fas fa-arrow-left"></i> Retroceder
                </a>
                
                <button onclick="location.reload()" class="btn btn-outline">
                    <i class="fas fa-redo"></i> Recarregar
                </button>
            </div>

            <div class="gold-divider"></div>

            <div class="footer-section">
                <div class="footer-info">
                    <div class="footer-item">
                        <i class="fas fa-headset"></i>
                        <span>Suporte: <strong>suporte@b7uno.com</strong></span>
                    </div>
                    <div class="footer-item">
                        <i class="fas fa-phone"></i>
                        <span>Urgente: <strong>+351 800 777 888</strong></span>
                    </div>
                    <div class="footer-item">
                        <i class="fas fa-user-tie"></i>
                        <span>Administração: <strong>admin@b7uno.com</strong></span>
                    </div>
                </div>
                
                <div class="security-notice">
                    <i class="fas fa-shield-check"></i>
                    Sistema B7Uno Casino • Segurança Máxima • Auditoria Ativa
                </div>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Efeito de partículas douradas
            const container = document.querySelector('.error-container');
            const goldColors = [
                'rgba(212, 175, 55, 0.6)',
                'rgba(255, 215, 0, 0.6)',
                'rgba(245, 208, 66, 0.6)'
            ];
            
            for (let i = 0; i < 15; i++) {
                const particle = document.createElement('div');
                particle.style.position = 'absolute';
                const size = Math.random() * 3 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                particle.style.background = goldColors[Math.floor(Math.random() * goldColors.length)];
                particle.style.borderRadius = '50%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.zIndex = '0';
                particle.style.opacity = Math.random() * 0.5 + 0.1;
                particle.style.animation = `floatGold ${Math.random() * 15 + 10}s linear infinite`;
                particle.style.boxShadow = '0 0 10px rgba(212, 175, 55, 0.5)';
                
                container.appendChild(particle);
            }
            
            // Adicionar animação CSS
            const style = document.createElement('style');
            style.textContent = `
                @keyframes floatGold {
                    0% { transform: translate(0, 0) rotate(0deg); }
                    25% { transform: translate(${Math.random() * 100 - 50}px, ${Math.random() * 50 - 25}px) rotate(90deg); }
                    50% { transform: translate(${Math.random() * 100 - 50}px, ${Math.random() * 50 - 25}px) rotate(180deg); }
                    75% { transform: translate(${Math.random() * 100 - 50}px, ${Math.random() * 50 - 25}px) rotate(270deg); }
                    100% { transform: translate(0, 0) rotate(360deg); }
                }
            `;
            document.head.appendChild(style);
            
            // Log do erro
            console.warn('🚨 B7Uno Casino Admin Error:', {
                code: <%= error && error.status ? error.status : 500 %>,
                time: new Date().toISOString(),
                path: window.location.pathname
            });
        });
    </script>
</body>
</html>